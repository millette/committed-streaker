<%- include('partials/doctype') %>
<div class="row column">
<div class="row">
<div class="column medium-6 lead">
<p>Êtes-vous un ou une <i>committed streaker</i>?</p>
<p>Découvrez les programmeurs et programmeuses du Québec.</p>
</div>
<div class="column medium-6">
<p><i>Committed streaker</i> est un service du <a href="http://www.rollodeqc.com/">RoLLodeQc</a>,
le bottin des codeurs Québécois.</p>
<p>Prenez part aux 5000 personnes du Québec qui ont contribué sur Github
au cours des 365 derniers jours.</p>
</div>
</div>

<% if (credentials && credentials.username) { %>
<p>Voir <a href="/me">votre profile</a> (<%= credentials.username%>).</p>

<% } else { %>
<%- include('partials/login') %>
<% } %>

<% if (typeof load === 'object') { %>
<h2>load <small><a href="/load/json">JSON</a></small></h2>
<pre>
<%= JSON.stringify(load, null, ' ') %>
</pre>
<% } %>

</div>
<%- include('partials/bottom') %>
